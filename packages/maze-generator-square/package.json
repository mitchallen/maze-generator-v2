{
  "name": "maze-generator-square",
  "version": "0.1.24",
  "description": "square maze generator",
  "main": "dist/maze-generator-square.cjs.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "//": "Usage:> [PORT=8002] npm run web-server",
    "start": "node dist/maze-generator-square.cjs.js",
    "build": "node build.js",
    "test": "npm run build && mocha --recursive --timeout 20000",
    "test-square": "npm run build && mocha test/square/ --recursive --timeout 5000",
    "test-debug": "npm run build && mocha -g '@DEBUG' --recursive --timeout 20000",
    "web-server": "python -m SimpleHTTPServer $PORT || 8000"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/mitchallen/maze-generator-square.git"
  },
  "author": "Mitch Allen <npm@mitchallen.com> (http://mitchallen.com)",
  "license": "MIT",
  "private": true,
  "dependencies": {
    "connection-grid-square": "^0.1.17",
    "maze-generator-core": "^0.1.13"
  },
  "devDependencies": {
    "esbuild": "^0.25.12",
    "mocha": "^11.7.6",
    "should": "^10.0.0"
  },
  "engines": {
    "node": ">=20",
    "npm": ">=10"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  }
}
