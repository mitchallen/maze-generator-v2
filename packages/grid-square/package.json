{
  "name": "grid-square",
  "version": "0.1.9",
  "description": "2D square grid",
  "main": "dist/grid-square.cjs.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "start": "node dist/grid-square.cjs.js",
    "build": "node build.js",
    "test": "npm run build && mocha --recursive --timeout 20000",
    "coverage": "npm run build && c8 --check-coverage --100 --include='src/**' --reporter=text mocha --recursive --timeout 20000",
    "test-debug": "npm run build && mocha -g '@DEBUG' --recursive --timeout 20000",
    "web-server": "python3 -m http.server ${PORT:-8000}"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/mitchallen/grid-square.git"
  },
  "author": "Mitch Allen <npm@mitchallen.com> (http://mitchallen.com)",
  "license": "MIT",
  "private": true,
  "dependencies": {
    "grid-core": "^0.1.10"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "mocha": "^12.0.2",
    "should": "^13.2.3"
  },
  "engines": {
    "node": ">=20",
    "npm": ">=10"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  }
}
