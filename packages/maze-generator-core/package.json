{
  "name": "maze-generator-core",
  "version": "0.1.13",
  "description": "maze generator core",
  "main": "dist/maze-generator-core.cjs.js",
  "scripts": {
    "start": "node dist/maze-generator-core.cjs.js",
    "build": "node build.js",
    "test": "npm run build && mocha --recursive --timeout 20000",
    "test-debug": "npm run build && mocha -g '@DEBUG' --recursive --timeout 20000"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/mitchallen/maze-generator-core.git"
  },
  "author": "Mitch Allen <npm@mitchallen.com> (http://mitchallen.com)",
  "license": "ISC",
  "private": true,
  "devDependencies": {
    "connection-grid-square": "^0.1.17",
    "esbuild": "^0.25.12",
    "mocha": "^11.7.6",
    "should": "^10.0.0",
    "supertest": "^7.1.3"
  },
  "engines": {
    "node": ">=20",
    "npm": ">=10"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  }
}
