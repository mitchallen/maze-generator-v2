{
  "name": "connection-grid-square",
  "version": "0.1.17",
  "description": "Connection grid for square grids",
  "main": "dist/connection-grid-square.cjs.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "start": "node dist/connection-grid-square.cjs.js",
    "build": "node build.js",
    "test": "npm run build && mocha --recursive --timeout 20000",
    "coverage": "npm run build && c8 --check-coverage --100 --include='src/**' --reporter=text mocha --recursive --timeout 20000",
    "test-square": "npm run build && mocha test/square/ --recursive --timeout 5000",
    "test-debug": "npm run build && mocha -g '@DEBUG' --recursive --timeout 20000"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/mitchallen/connection-grid-square.git"
  },
  "author": "Mitch Allen <npm@mitchallen.com> (http://mitchallen.com)",
  "license": "MIT",
  "private": true,
  "dependencies": {
    "connection-grid-core": "^0.1.22",
    "grid-square": "^0.1.9"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "mocha": "^12.0.2",
    "should": "^13.2.3"
  },
  "engines": {
    "node": ">=20",
    "npm": ">=10"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  }
}
